import styled from '@emotion/styled'
import { Link as GLink } from 'gatsby'

require('typeface-averta')

export const Link = styled(GLink)`
  font-family: 'Averta';
  display: block;
  margin-top: 30px;
`
